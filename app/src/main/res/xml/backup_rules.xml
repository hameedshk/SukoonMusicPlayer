<?xml version="1.0" encoding="utf-8"?>
<!-- Backup rules for Sukoon Music Player (API ≤ 30) -->
<!-- Prevents sensitive personal data from being backed up to Google Drive -->
<full-backup-content>
    <!-- Exclude Room database (contains personal listening history, playlists, search history) -->
    <exclude domain="database" path="sukoon_database" />

    <!-- Exclude DataStore preferences (contains anonymous UUID, private session state) -->
    <exclude domain="file" path="datastore/" />

    <!-- Allow UI preferences to be backed up (theme, language, EQ settings are not sensitive) -->
</full-backup-content>